node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Hearth runtime data (SQLite DB, logs, TLS certs) -- never committed
/data/
*.db
*.db-shm
*.db-wal
*.log

# M7 scratch: local-only clones/working copies (umbrel-apps checkout for
# lint, etc.) -- never committed, read-only reference material
/.scratch/

