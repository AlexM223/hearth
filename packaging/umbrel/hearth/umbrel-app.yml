manifestVersion: 1
id: hearth
category: bitcoin
name: Hearth
version: "0.1.0"
tagline: Come navigate Bitcoin with me
description: >-
  Hearth is the always-watching Bitcoin wallet for the people you trust: you
  run the node -- the strong, warm core -- and you invite family and friends
  to come navigate Bitcoin with you. One verified chain, one mempool, one
  shared view; every member keeps their own keys; the watchtower tells each
  of them the moment their money moves.


  What's aboard: one unified wallet engine for watch-only, single-sig, and
  multisig spends -- build, review, and broadcast a PSBT through a single
  code path, signing with whatever external signer you already trust; invite
  by link so a guest sets their own password and lands on a captain-branded
  welcome, never a shared secret; a pool-aware block explorer built entirely
  from your own node; an off-by-default solo Stratum mining pool with a
  first-party dashboard (hashrate, best-share trophy, per-worker table); and
  the watchtower -- fail-closed, SPV-verified push notifications over email,
  Telegram, ntfy, Nostr, or webhook the moment money moves, never on a price
  tick.


  Signing itself happens right in the browser: plug in a Ledger or Trezor,
  or go air-gapped with a signed-transaction file or an animated QR code --
  whichever signer you already trust. A couple of ports beyond the usual
  Umbrel app page are worth knowing about before you install. Hearth also
  publishes a self-signed HTTPS listener on port 4489, which is what gives
  the browser the secure connection that Ledger and camera QR-scanning
  need (Trezor's popup and file-based signing work over the app's usual
  address, no extra step); your browser will ask you to accept its
  certificate the first time it sends you there, and that's expected. If
  you turn on the solo mining pool (off by default), your miner (e.g. a
  Bitaxe) connects to this box on port 3343 (standard difficulty) or 3345
  (ASIC-floor difficulty); nothing listens on either port until mining is
  enabled.


  Runs 100% on your own node -- no cloud, no telemetry, nothing leaves this box.
releaseNotes: >-
  0.1.0 -- first public release. The full M0-M6 build: the Ingle design
  system (dark default, warm-parchment light); the node:sqlite-backed
  module-bounded server; the one unified wallet engine (watch-only,
  single-sig, 2-of-3+ multisig, PSBT build/review/broadcast through a single
  code path); invite-by-link members with Owner/Member/Guest roles and a
  captain-branded come-aboard flow; a pool-aware own-node block explorer; an
  off-by-default solo Stratum V1 mining pool with a first-party dashboard;
  and the five-channel fail-closed watchtower notifier. See DECISIONS.md for
  the full build order and the M7 store-readiness checklist.
developer: Alex Martinez
website: https://github.com/AlexM223/hearth
dependencies:
  - bitcoin
  - electrs
repo: https://github.com/AlexM223/hearth
support: https://github.com/AlexM223/hearth/issues
port: 3252
gallery:
  - 1.jpg
  - 2.jpg
  - 3.jpg
  - 4.jpg
  - 5.jpg
path: ""
defaultUsername: "admin@hearth.local"
deterministicPassword: true
backupIgnore:
  - data/logs
  - data/tls
  - data/secret.key
submitter: Alex Martinez
# No official-store PR yet: Hearth ships through its own new community store
# repo (DECISIONS.md §5.5, `AlexM223/hearth-app-store`), not a
# getumbrel/umbrel-apps submission, so there is no upstream PR to reference.
# Filled in if/when Alex ever submits to the official store instead.
submission: ""
